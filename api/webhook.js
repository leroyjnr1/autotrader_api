export default async function handler(req, res) { if (req.method !== 'POST') { return res.status(405).json({ error: 'Method not allowed' }); } try { const carData = req.body; console.log('Received car data:', carData); const transformed = { receivedAt: new Date().toISOString(), totalListings: carData.length, cars: carData.map(car => ({ id: car.id, make: car.make, model: car.model, price: car.price, year: car.year })) }; res.status(200).json({ success: true, message: 'Data received and processed', itemsReceived: carData.length }); } catch (error) { console.error('Error processing webhook:', error); res.status(500).json({ error: 'Processing failed' }); } }
